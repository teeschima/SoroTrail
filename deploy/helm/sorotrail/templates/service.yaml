apiVersion: v1
kind: Service
metadata:
  name: {{ include "sorotrail.fullname" . }}
  labels:
    {{- include "sorotrail.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sorotrail.selectorLabels" . | nindent 4 }}
